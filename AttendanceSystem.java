// Adaptee: existing attendance system with its own method
public class AttendanceSystem {
    public void markAttendance(String studentId, String section) {
        System.out.println("Attendance marked for student " + studentId + " in section " + section + ".");
    }
}