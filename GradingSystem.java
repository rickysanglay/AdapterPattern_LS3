// Adaptee: existing grading system with its own method
public class GradingSystem {
    public void recordGrades(String courseCode, double grade) {
        System.out.println("Grades recorded: " + courseCode + " -> " + grade);
    }
}